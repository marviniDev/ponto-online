import styled from "styled-components";

export const MenuTopContainer = styled.nav`
  display: flex;
  justify-content: space-between;
  align-items: center;
  position: relative;
  width: 100%;
  min-height: 3.5em;
  background-color: var(--cor-main);
  padding: 8px 16px;
  box-shadow: rgba(0, 0, 0, 0.16) 0px 1px 4px;
  color: #fff;
`;